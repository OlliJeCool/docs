---
title: 'unclaimProduct'
description: 'Uncalims a product - unlinks it from the logged in user who is unclaiming it.'
---
    
    ## Arguments
    
    <ParamField body="productPublicId" type="String!"/> 

    
    ## Returns

    <ParamField body="unclaimProduct" type="Boolean!"/>
    <Info>If the `Product` does not exists, this mutation returns a `NotFoundException`. If the user trying to unclaim the `Product` is not the owner, this mutation returns a `UnauthorizedException`.</Info>
    <RequestExample>
    ```graphql Request
    mutation ($productPublicId: String!) {
        unclaimProduct(productPublicId: $productPublicId)
    }
    ```
    ```json Arguemnts
    {
        "productPublicId": "productPublicId"
    }
    ```
    ```json Headers
    {
        "Authorization": "Bearer <token>"
    }
    ```
    </RequestExample>

    <ResponseExample>
    ```json Response
    {
        "unclaimProduct": true
    }
    ```
    </ResponseExample>