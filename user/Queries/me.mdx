---
title: 'me'
description: 'Returns selected data of the currently logged in user.'
---
    
    ## Arguments
    
    <ParamField body="select" type="FollowingSelect">
    Input object determining whether to return counts of followers and followings. When querying the followers fields of user this input has to be provided, otherwise the query will fail.
    <Expandable title="fields">
    <ResponseField name="followedBy" type="Boolean">Whether to select the count of users followers.</ResponseField>
<ResponseField name="following" type="Boolean">Whether to select the count of users followings.</ResponseField>
    </Expandable>
    </ParamField> 

    
    ## Returns
    <Info>If the currently logged in user is not in the database, this mutation returns a `NotFoundException`.</Info>
    <ParamField body="me" type="User">
    User model
    <Expandable title="fields">
    <ResponseField name="about" type="String">Information about the user.</ResponseField>
<ResponseField name="backgrounds" type="[Background!]!">Backgrounds owned by user.</ResponseField>
<ResponseField name="followedBy" type="[User!]!">Users following user.</ResponseField>
<ResponseField name="followers" type="FollowingCounts!"/>
<ResponseField name="following" type="[User!]!">Users followed by user.</ResponseField>
<ResponseField name="from" type="String">User location.</ResponseField>
<ResponseField name="id" type="String!">UUID generated by Auth0 and assigned on creation.</ResponseField>
<ResponseField name="name" type="String!">User name, not unique.</ResponseField>
<ResponseField name="products" type="[Product!]!">Products owned by user.</ResponseField>
<ResponseField name="selectedBackground" type="Background!">The background selected by the user to be displayed on their card.</ResponseField>
<ResponseField name="socials" type="[Social!]!">Socials owned by user.</ResponseField>
<ResponseField name="verified" type="Boolean!">Defines if the user is verified by Decode Fashion.</ResponseField>
    </Expandable>
    </ParamField>
    
    <RequestExample>
    ```graphql Request
    query ($select: FollowingSelect) {
      me(select: $select) {
        about
		from
		id
		name
		verified  
        }
    }
    ```
    ```json Arguemnts
    {
    "select": {
        "followedBy": true,
        "following": true
        }
    }
    ```
    ```json Headers
    {
        "Authoriaztion": "Bearer <token>"
    }
    ```
    </RequestExample>
    <ResponseExample>
    ```json Response
    {
    "me": {
        "about": "about",
        "from": "from",
        "id": "08a16b83-9094-4e89-8c05-2ccadd5c1c7e",
        "name": "A name",
        "verified": true
        }
    }
    ```
    </ResponseExample>
    