---
title: 'registerUser'
description: 'Registers a new user.'
---
    
    ## Arguments
    
    <ParamField body="registerDto" type="RegisterDto">
    
    <Expandable title="fields">
    <ResponseField name="email" type="String!"/>
<ResponseField name="newUserId" type="String!"/>
<ResponseField name="username" type="String!"/>
    </Expandable>
    </ParamField> 

    
    ## Returns
    <Info>If the `User` already exists, this muatation returns a `ConflictException`.</Info>
    <ParamField body="registerUser" type="String!"/>

    <RequestExample>
    ```graphql Request
    mutation ($registerDto: RegisterDto!) {
        registerUser(registerDto: $registerDto)
    }
    ```
    ```json Arguments
    {
      "registerDto": {
        "email": "test-email@yourcompany.com",
        "newUserId": "newUserId",
        "username": "username"
      }
    }
    ```
    ```json Headers
    {
        "Authorization": "Bearer <token>"
    }
    ```
    </RequestExample>

    <ResponseExample>
    ```json Response
    {
        "registerUser": "registerUser"
    }
    ```
    </ResponseExample>