---
title: 'unfollowUser'
description: 'Allows the currently authenticated user to unfollow another user.'
---
    
    ## Arguments
    
    <ParamField body="targetId" type="String!"/> 

    
    ## Returns
    <Info>If the target is not followed, this mutation returns a `NotFoundException`.</Info>
    <ParamField body="unfollowUser" type="Boolean!"/>


<RequestExample>
```graphql Request
mutation ($targetId: String!) {
  unfollowUser(targetId: $targetId)
}
```
```json Arguemnts
{
  "targetId": "targetId"
}
```
```json Headers
{
    "Authorization": "Bearer <token>"
}
```
</RequestExample>

<ResponseExample>
```json Response
{
  "unfollowUser": true
}
```
</ResponseExample>