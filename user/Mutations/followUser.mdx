---
title: 'followUser'
description: 'Allows the currently authenticated user to follow another user.'
---
    
    ## Arguments
    
    <ParamField body="targetId" type="String!"/> 

    
    ## Returns

    <ParamField body="followUser" type="Boolean!"/>

    <RequestExample>
    ```graphql Request
    mutation ($targetId: String!) {
        followUser(targetId: $targetId)
    }
    ```
    ```json Arguemnts
    {
        "targetId": "targetId"
    }
    ```
    ```json Headers
    {
        "Authorization": "Bearer <token>"
    }
    ```
    </RequestExample>

    <ResponseExample>
    ```json Response
    {
        "followUser": true
    }
    ```
    </ResponseExample>