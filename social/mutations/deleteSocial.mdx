---
title: 'deleteSocial'
description: 'Deletes the given Social.'
---
    
    ## Arguments
    
    <ParamField body="socialId" type="String!"/> 

    
    ## Returns
    <Info>If the `Product` does not exist, this mutation returns a `NotFoundException`.</Info>
    <ParamField body="deleteSocial" type="Boolean!"/>

    <RequestExample>
    ```graphql Request
    mutation ($socialId: String!) {
        deleteSocial(socialId: $socialId)
    }
    ```
    ```json Arguments
    {
        "socialId": "socialId"
    }
    ```
    ```json Headers
    {
        "Authorization": "Bearer <token>"
    }
    ```
    </RequestExample>

    <ResponseExample>
    ```json Response
    {
        "deleteSocial": true
    }
    ```
    </ResponseExample>